---
name: "search_kb"
pack: "st2-community"
action_ref: "st2-community.search_kb"
formats:
  - display: "kb {{ query }}"
    representation:
      - "((search|get)( (me|a|the))? )?kb( for)? {{ query }}"
description: "Search the StackStorm Knowledge Base for articles"

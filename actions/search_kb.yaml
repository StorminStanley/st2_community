---
  name: "search_kb"
  pack: "st2-community"
  runner_type: "action-chain"
  entry_point: "chains/search_kb.yaml"
  description: "Perform lookups on the online KB and show them to users via ChatOps"
  enabled: true
  parameters:
    query:
      type: "string"
      required: true
